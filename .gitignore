# Файл workflow ждёт права `workflow` у токена gh (см. переписку 06.08):
# GitHub отклоняет пуш от OAuth-приложения без этой области. Пока не выдана —
# держим файл вне индекса, иначе он блокирует ЛЮБОЙ пуш в этот репозиторий.
# Выдать: gh auth refresh -s workflow → затем убрать эту строку и закоммитить.
.github/
